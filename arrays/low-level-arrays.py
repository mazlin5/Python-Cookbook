# lookup o(1)
# push (1)
# insert or delete o(n)

#  referential array - use an array of object References
#  counter = [0] *8 - all eight cells reference same object
#  referenced object is immutable counter[2] += 1 - computes new integer where [2] = 1
#  [0] = 'Rene' [2] = 'Janet'

strings = ['a', 'b', 'c', 'd']
# 4*4 = 16 bytes of space




